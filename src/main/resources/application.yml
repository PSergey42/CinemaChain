spring:
  jpa:
    database: POSTGRESQL
    show-sql: false
  datasource:
    driverClassName: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/cinemaChain
    username: postgres
    password: admin
